[pytest]
testpaths = backend/tests
pythonpath = backend
asyncio_mode = auto
addopts = -v --tb=short
markers =
    unit: pure unit tests (no I/O)
    integration: tests with filesystem, network, or subprocess
